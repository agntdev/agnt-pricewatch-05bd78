[
  {
    "name": "/owner shows owner-only message when not owner",
    "steps": [
      {
        "send": { "text": "/owner" },
        "expect": [
          {
            "method": "sendMessage",
            "payload": { "text": "This command is only available to the bot owner." }
          }
        ]
      }
    ]
  }
]
